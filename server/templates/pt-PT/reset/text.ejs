<%= saudacao %> <%= name %>,

Foi feito um pedido de uma nova senha associada a este email para o site <%= site %>

Se foi você que fez o pedido, siga a ligação seguinte para lhe ser enviada uma nova senha, invalidando a anterior.

<%= site %>/reset/<%= lang %>/<%= token %>

Se não pediu uma nova senha, pode ignorar esta mensagem, pois nenhuma ação será tomada.

Votos de um bom dia!